# DLT framework
dlt[pyiceberg]>=1.0.0

# PostgreSQL driver (binary wheel, no compilation needed)
psycopg2-binary>=2.9.0

# PyIceberg for Iceberg table format
pyiceberg>=0.6.0

# Additional dependencies
python-dotenv>=1.0.0
pyarrow>=14.0.0

# Use only binary wheels to avoid compilation
--prefer-binary
